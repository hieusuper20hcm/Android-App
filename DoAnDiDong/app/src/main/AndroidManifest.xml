<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doandidong">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DoAnDiDong"
        android:usesCleartextTraffic="true">
        <activity android:name=".Accessories"></activity>
        <activity android:name=".UpdatePassword" />
        <activity android:name=".ForgetPasword" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".ShirtActivity" />
        <activity android:name=".TrouserActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".HistoryDetail" />
        <activity android:name=".DetailActivity" />
        <activity android:name=".CartActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>